{
  "name": "nextfs-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build:fable": "dotnet fable src/NextFsApp.fsproj -o .fable -e .js",
    "watch:fable": "dotnet fable watch src/NextFsApp.fsproj -o .fable -e .js --runWatch \"npm run scan && npm run gen:wrappers\"",
    "scan": "node tools/nextfs-scan.mjs src/NextFsApp.fsproj",
    "gen:wrappers": "node tools/nextfs-entry.mjs nextfs.entries.json",
    "sync:app": "npm run build:fable && npm run scan && npm run gen:wrappers",
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^16.2.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
